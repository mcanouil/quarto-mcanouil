---
title: "Examples"
subtitle: "Worked front matter, one page per topic."
description: "How to configure the three formats, write the components, and organise a document, with one page for each group of options."
---

The [rendered report](preview.pdf) on this site is built by the format as the site is published.

An option belongs in one of three places, and the pages below are grouped the same way.

- A format option goes under `format: mcanouil-html:`, `mcanouil-typst:`, or `mcanouil-revealjs:`.
- A document option goes under `extensions: mcanouil:`.
- A block attribute goes on the element itself.

## The pages

| Page | What it covers |
| --- | --- |
| [Components](examples/components.qmd) | The `divider`, `value-box`, `progress`, and `badge` shortcodes, and the colour and icon values they accept. |
| [Panels and cards](examples/panels-and-cards.qmd) | The container divs, the two ways a card gets a footer, and where a title comes from. |
| [Special sections](examples/special-sections.qmd) | Appendices, supplementary material, references, and tabsets in Typst output. |
| [HTML format](examples/html-format.qmd) | The layout style, the three title block groups, and what a standalone document must set for itself. |
| [Typst format](examples/typst-format.qmd) | Document types, page decoration, section pages, and sending a class to a Typst function. |
| [Reveal.js format](examples/revealjs-format.qmd) | Section outlines, ordinal dates, and the two options for laying a deck out. |
| [Code windows](examples/code-windows.qmd) | Window chrome per project and per block, and the three Typst hot-fixes. |

: What each page of this section shows. {.striped .hover tbl-colwidths="[26,74]"}

## A website

```yaml
project:
  type: mcanouil
```

The project type supplies the navbar, sidebar, footer, and favicon, and sets `mcanouil-html` as the default format, so a site needs nothing else to be branded.
It also supplies a value for several document options, which a standalone document has to set for itself.

## A document

```yaml
---
title: "My Document"
format: mcanouil-html
---
```

## A presentation

```yaml
---
title: "My Presentation"
format:
  mcanouil-revealjs:
    brand-mode: dark
---
```

## Source

The repository ships a starting point you can copy: [`example.qmd`](https://github.com/mcanouil/quarto-mcanouil/blob/main/example.qmd).
